import React from 'react'
import {RouteComponentProps} from '@reach/router';

const NotFound = ({...RouteComponentProps}) => {
    return (
        <div>
            <h1>NOT FOUND</h1>
        </div>
    )
}

export default NotFound
